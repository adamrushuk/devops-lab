user_request_body:
  userId: "{{ nexus_user_username }}"
  firstName: "demo"
  lastName: "user"
  emailAddress: "{{ nexus_user_username }}@domain.com"
  status: "active"
  password: "{{ nexus_user_password }}"
  roles: [
    "nx-admin"
  ]

anon_user_request_body:
  enabled: "true"
  userId: "anonymous"
  realmName: "NexusAuthorizingRealm"
