docker_repo_request_body:
  name: docker-repo
  online: true
  storage:
    blobStoreName: default
    strictContentTypeValidation: true
    writePolicy: ALLOW_ONCE
  docker:
    v1Enabled: false
    forceBasicAuth: false
    httpPort: 5000
